% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/default-credential.R
\name{get_user_token}
\alias{get_user_token}
\title{Get an Access Token for the Signed-In User}
\usage{
get_user_token(
  scope = NULL,
  tenant_id = NULL,
  client_id = default_azure_cli_client_id(),
  use_cache = "disk",
  offline = TRUE
)
}
\arguments{
\item{scope}{Optional character string specifying the authentication scope.}

\item{tenant_id}{Optional character string specifying the tenant ID for
authentication.}

\item{client_id}{Optional character string specifying the client ID to
authenticate against. Defaults to \code{\link[=default_azure_cli_client_id]{default_azure_cli_client_id()}}, the
public Azure CLI client, rather than to \code{AZURE_CLIENT_ID}: on a host
configured for a service identity that variable holds the \emph{workload's}
application, which is not the application a person signs in to.}

\item{use_cache}{Character string indicating the caching strategy. Defaults
to \code{"disk"}. Options include \code{"disk"} for disk-based caching or \code{"memory"}
for in-memory caching.}

\item{offline}{Logical. If \code{TRUE}, adds 'offline_access' to the scope to request a 'refresh_token'.
Defaults to \code{TRUE}.}
}
\value{
An \code{\link[httr2:oauth_token]{httr2::oauth_token()}} object.
}
\description{
Retrieves an authentication token for the person signed in on this machine,
skipping the service identities that \code{\link[=get_token]{get_token()}} would try first.
}
\details{
\code{\link[=get_token]{get_token()}} walks the whole \code{\link[=default_credential_chain]{default_credential_chain()}}, where
\code{workload_identity} and \code{managed_identity} come before \code{azure_cli}. On a host
configured for a service identity - an AKS pod setting
\code{AZURE_FEDERATED_TOKEN_FILE}, a CI runner with OIDC - that means
\code{\link[=get_token]{get_token()}} hands back the \emph{workload's} identity even when a person is
sitting at the console. \code{get_user_token()} restricts the chain to the
credentials that represent a person, tried in the same order the default
chain uses them:
\enumerate{
\item Azure CLI Credential - the current \verb{az login} session
\item Authorization Code Credential - interactive browser sign-in
\item Device Code Credential - interactive device code flow
}

The two interactive flows are skipped in non-interactive sessions, so on an
unattended host this resolves to the Azure CLI login or fails. \code{client_secret}
is deliberately absent from the arguments: it authenticates an application,
not a user.
}
\examples{
\dontrun{
scope <- "https://management.azure.com/.default"

# on a host configured for workload identity, this is the workload
get_token(scope = scope)

# ...while this is whoever ran `az login`
get_user_token(scope = scope)
}

}
\seealso{
\code{\link[=get_token]{get_token()}}, \code{\link[=az_cli_get_token]{az_cli_get_token()}}, \code{\link[=default_credential_chain]{default_credential_chain()}}
}
