% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/az-dataset.R
\name{az_catalog}
\alias{az_catalog}
\title{Azure Storage dataset catalog}
\usage{
az_catalog(datasets = list())
}
\arguments{
\item{datasets}{A list of \link{az_dataset} objects.}
}
\value{
An \code{az_catalog} S7 object.
}
\description{
An S7 class holding an ordered collection of \link{az_dataset} objects with
unique \code{name}s.

A catalog can be indexed by dataset name with \code{[[}, and supports \code{names()}
and \code{length()}.
}
\examples{
ds <- az_dataset(
  name = "orders",
  scheme = "abfss",
  container = "raw",
  storage = list(prod = "stprod001"),
  path = "sales/orders",
  format = "delta"
)
catalog <- az_catalog(datasets = list(ds))

catalog[["orders"]]
names(catalog)
length(catalog)
}
