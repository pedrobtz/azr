% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/defaults.R
\name{set_azr_defaults}
\alias{set_azr_defaults}
\title{Set package-level Azure defaults}
\usage{
set_azr_defaults(
  host = .azr_defaults$host,
  client_id = .azr_defaults$client_id,
  tenant_id = .azr_defaults$tenant_id
)
}
\arguments{
\item{host}{A character string specifying the Azure authority host, or
\code{NULL} to clear any previously set override.}

\item{client_id}{A character string specifying the Azure client ID, or
\code{NULL} to clear any previously set override.}

\item{tenant_id}{A character string specifying the Azure tenant ID, or
\code{NULL} to clear any previously set override.}
}
\value{
Invisibly returns the previous values as a named list.
}
\description{
Overrides the built-in fallback values used by \code{\link[=default_azure_host]{default_azure_host()}},
\code{\link[=default_azure_client_id]{default_azure_client_id()}}, and \code{\link[=default_azure_tenant_id]{default_azure_tenant_id()}} when the
corresponding environment variable is not set. Pass \code{NULL} to a parameter
to clear a previously set override.

The priority order for each default is:
\enumerate{
\item Package-level override set by \code{set_azr_defaults()} (highest)
\item Environment variable (\code{AZURE_AUTHORITY_HOST}, \code{AZURE_CLIENT_ID},
\code{AZURE_TENANT_ID})
\item Built-in fallback (lowest)
}
}
\examples{
# Override the authority host for Azure Government
set_azr_defaults(host = "login.microsoftonline.us")

# Clear the override
set_azr_defaults(host = NULL)
}
