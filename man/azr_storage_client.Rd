% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/azure-storage-client.R
\name{azr_storage_client}
\alias{azr_storage_client}
\title{Create an Azure Storage API Client}
\usage{
azr_storage_client(
  storageaccount,
  filesystem,
  scopes = ".default",
  chain = NULL,
  ...
)
}
\arguments{
\item{storageaccount}{A character string specifying the Azure Storage account name.}

\item{filesystem}{A character string specifying the filesystem (container) name.}

\item{scopes}{A character string specifying the OAuth2 scope suffix. Defaults to
\code{".default"}, which requests all permissions the app has been granted.}

\item{chain}{A \link{credential_chain} instance for authentication. If NULL,
a default credential chain will be created using \link{DefaultCredential}.}

\item{...}{Additional arguments passed to the \link{api_storage_client} constructor.}
}
\value{
An \link{api_storage_client} object configured for Azure Storage with methods
to interact with files and directories.
}
\description{
Creates a configured client for Azure Data Lake Storage Gen2 (ADLS Gen2) REST API.
This function returns an \link{api_storage_client} object that provides methods for
interacting with files and directories in Azure Storage.
}
\details{
The function creates an Azure Storage client that uses the Data Lake Storage Gen2
REST API endpoints. The base URL is constructed as:
\verb{https://\{storageaccount\}.dfs.core.windows.net}

The client is configured with:
\itemize{
\item Azure authentication using credential provider
\item Access to a specific filesystem (container)
\item Methods for listing files and directories
}
}
\examples{
\dontrun{
# Create a storage client
storage <- azr_storage_client(
  storageaccount = "mystorageaccount",
  filesystem = "mycontainer"
)

# List files in the root directory
files <- storage$list_files()

# List files in a specific path
files <- storage$list_files(path = "data/folder1")

# List files recursively
files <- storage$list_files(path = "data", recursive = TRUE)
}
}
