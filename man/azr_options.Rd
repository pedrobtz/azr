% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{azr_options}
\alias{azr_options}
\title{List all azr options and their current values}
\usage{
azr_options(mask = TRUE)
}
\arguments{
\item{mask}{Logical. When \code{TRUE} (default), sensitive option values are
shown as \code{"<hidden>"} when set.}
}
\value{
Invisibly, a \link{data.frame} with columns \code{option}, \code{value}, \code{source},
\code{env_var}, \code{env_value}, and \code{default}.
}
\description{
Prints every azr option and invisibly returns a
\link{data.frame} of the same information. The resolution order is: a value set
for the session -> \verb{options(azr.*)} -> the option's environment variable ->
a built-in default.\tabular{lllll}{
   Name \tab R option \tab Env variable \tab Default \tab Description \cr
   \code{"chain_verbose"} \tab \code{azr.chain_verbose} \tab \code{AZR_CHAIN_VERBOSE} \tab \code{FALSE} \tab Verbose credential-chain discovery \cr
   \code{"api_verbose"} \tab \code{azr.api_verbose} \tab \code{AZR_API_VERBOSE} \tab \code{FALSE} \tab Verbose api_client request/response \cr
   \code{"cli_auto_login"} \tab \code{azr.cli_auto_login} \tab \code{AZR_CLI_AUTO_LOGIN} \tab \code{FALSE} \tab Auto Azure CLI login \cr
   \code{"dataset_tier"} \tab \code{azr.dataset_tier} \tab \code{AZR_DATASET_TIER} \tab \code{"prod"} \tab Default tier for \code{\link[=azr_dataset_uri]{azr_dataset_uri()}} \cr
}
}
\examples{
azr_options()

# Set for the session
options(azr.chain_verbose = TRUE)

# Or via environment variable (before starting R)
# AZR_CHAIN_VERBOSE=true
}
