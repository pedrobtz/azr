% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spark-conf.R
\name{azure_spark_storage_conf}
\alias{azure_spark_storage_conf}
\title{Azure Storage Spark / Hadoop configuration}
\usage{
azure_spark_storage_conf(
  auth_type = c("refresh_token", "client_secret", "workload_identity",
    "managed_identity", "shared_key"),
  storage_account = NULL,
  tenant_id = default_azure_tenant_id(),
  client_id = default_azure_client_id(),
  client_secret = default_azure_client_secret(),
  refresh_token = default_refresh_token(),
  account_key = NULL,
  token_file = default_federated_token_file(),
  authority_host = default_azure_host(),
  prefix = "spark.hadoop"
)
}
\arguments{
\item{auth_type}{Authentication type. One of \code{"refresh_token"} (default),
\code{"client_secret"}, \code{"workload_identity"}, \code{"managed_identity"}, or
\code{"shared_key"}.}

\item{storage_account}{Optional storage account, either a short name
(\code{"myacct"}) or a fully qualified host (\code{"myacct.dfs.core.windows.net"}).
When \code{NULL} (default) the keys are applied globally to all accounts. When a
short name is supplied the endpoint suffix is derived from
\code{authority_host}. Required for \code{auth_type = "shared_key"}.}

\item{tenant_id}{Azure tenant ID. Defaults to \code{\link[=default_azure_tenant_id]{default_azure_tenant_id()}}.}

\item{client_id}{Azure application (client) ID. Defaults to
\code{\link[=default_azure_client_id]{default_azure_client_id()}}. For \code{auth_type = "managed_identity"} this
selects a user-assigned identity (omit or leave at default for
system-assigned).}

\item{client_secret}{Client secret. Required when
\code{auth_type = "client_secret"}. Defaults to \code{\link[=default_azure_client_secret]{default_azure_client_secret()}}.}

\item{refresh_token}{Refresh token. Required when
\code{auth_type = "refresh_token"}. Defaults to \code{\link[=default_refresh_token]{default_refresh_token()}}.}

\item{account_key}{Storage account access key. Required when
\code{auth_type = "shared_key"}.}

\item{token_file}{Path to the federated service-account token file. Used
only when \code{auth_type = "workload_identity"}. Defaults to
\code{\link[=default_federated_token_file]{default_federated_token_file()}} (\code{AZURE_FEDERATED_TOKEN_FILE}).}

\item{authority_host}{Azure authority host (without scheme), e.g.
\code{"login.microsoftonline.com"}. Used to build the OAuth token endpoint for
\code{client_secret} and \code{refresh_token}, the MSI authority for
\code{workload_identity} and \code{managed_identity}, and to derive the storage
endpoint suffix for sovereign clouds. Defaults to \code{\link[=default_azure_host]{default_azure_host()}}.}

\item{prefix}{Optional prefix prepended to every returned key. Defaults to
\code{"spark.hadoop"}, the prefix Spark uses to forward properties to Hadoop.
Pass \code{NULL} to get the raw \verb{fs.azure.*} keys (e.g. for \code{core-site.xml} or
a \code{hadoopConfiguration()} call). A trailing dot in \code{prefix} is optional.}
}
\value{
A named list of class \code{azure_spark_config}. With the default
\code{prefix}, keys look like \verb{spark.hadoop.fs.azure.account.*}; with
\code{prefix = NULL}, keys look like \verb{fs.azure.account.*}. The \code{print} method
redacts sensitive values (account key, client secret, refresh token).
}
\description{
Builds the named list of Hadoop \verb{fs.azure.*} configuration keys required to
authenticate Apache Spark (or any ABFS-compatible runtime) against Azure Data
Lake Storage Gen2. With the default \code{prefix = "spark.hadoop"} the returned
list is ready to pass to \code{SparkSession.builder.config()}, a Databricks
cluster's Spark config, or \code{spark-defaults.conf} — Spark forwards
\verb{spark.hadoop.*} keys to Hadoop at FileSystem-init time. Pass \code{prefix = NULL}
to get the raw \verb{fs.azure.*} form, suitable for \code{core-site.xml} or a
\verb{sparkContext.hadoopConfiguration().set(...)} call.

Five authentication types are supported:
\describe{
\item{\code{"client_secret"}}{Service principal with client secret
(\code{ClientCredsTokenProvider}).}
\item{\code{"refresh_token"}}{Delegated user identity via a refresh token
(\code{RefreshTokenBasedTokenProvider}).}
\item{\code{"workload_identity"}}{Kubernetes workload identity via a projected
service-account token file (\code{WorkloadIdentityTokenProvider}). Requires
Hadoop 3.4.1+ / 3.5.0+ (HADOOP-18610). Stock Apache Spark 3.5 ships with
Hadoop 3.3.4, so this requires Spark 4.x or a runtime that bundles a
newer Hadoop (Databricks 14.3+ LTS, Synapse 3.4+).}
\item{\code{"managed_identity"}}{Azure managed identity via IMDS
(\code{MsiTokenProvider}). For Azure VMs, App Service, Functions, Container
Instances, and AKS pods without workload identity. Pass \code{client_id} to
select a user-assigned identity.}
\item{\code{"shared_key"}}{Storage account key (\code{SharedKey} auth type). Requires
\code{storage_account} and \code{account_key}; cannot be configured globally.}
}

Sovereign clouds (Azure US Government, Azure China) are supported by passing
the matching \code{authority_host} (e.g. \code{"login.microsoftonline.us"}). The
storage endpoint suffix used to scope keys to a specific account is derived
from the authority host. Alternatively, pass a fully qualified
\code{storage_account} like \code{"myacct.dfs.core.usgovcloudapi.net"} to override the
derivation entirely.
}
\examples{
# Global client-secret config (applies to all storage accounts)
azure_spark_storage_conf(
  auth_type = "client_secret",
  tenant_id = "my-tenant",
  client_id = "my-client",
  client_secret = "my-secret"
)

# Scoped to a specific storage account
azure_spark_storage_conf(
  auth_type = "client_secret",
  storage_account = "mystorageaccount",
  tenant_id = "my-tenant",
  client_id = "my-client",
  client_secret = "my-secret"
)

# Azure US Government sovereign cloud
azure_spark_storage_conf(
  auth_type = "client_secret",
  storage_account = "mystorageaccount",
  tenant_id = "my-tenant",
  client_id = "my-client",
  client_secret = "my-secret",
  authority_host = "login.microsoftonline.us"
)

\dontrun{
# Workload identity on AKS (reads env vars automatically)
azure_spark_storage_conf(auth_type = "workload_identity")

# Managed identity on an Azure VM (system-assigned)
azure_spark_storage_conf(auth_type = "managed_identity")
}
}
