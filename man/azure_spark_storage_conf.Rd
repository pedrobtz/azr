% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spark-conf.R
\name{azure_spark_storage_conf}
\alias{azure_spark_storage_conf}
\title{Azure Storage Spark / Hadoop configuration}
\usage{
azure_spark_storage_conf(
  type = c("client_secret", "refresh_token", "workload_identity"),
  storage = NULL,
  tenant_id = default_azure_tenant_id(),
  client_id = default_azure_client_id(),
  client_secret = default_azure_client_secret(),
  refresh_token = default_refresh_token(),
  token_file = default_federated_token_file(),
  oauth_host = default_azure_host()
)
}
\arguments{
\item{type}{Authentication type. One of \code{"client_secret"},
\code{"refresh_token"}, or \code{"workload_identity"}.}

\item{storage}{Optional storage account name (e.g. \code{"mystorageaccount"}).
When \code{NULL} (default) the keys are applied globally to all storage
accounts. When a name is supplied the keys are scoped to
\verb{<storage>.dfs.core.windows.net} only.}

\item{tenant_id}{Azure tenant ID. Defaults to \code{\link[=default_azure_tenant_id]{default_azure_tenant_id()}}.}

\item{client_id}{Azure application (client) ID. Defaults to
\code{\link[=default_azure_client_id]{default_azure_client_id()}}.}

\item{client_secret}{Client secret. Required when
\code{type = "client_secret"}. Defaults to \code{\link[=default_azure_client_secret]{default_azure_client_secret()}}.}

\item{refresh_token}{Refresh token. Required when
\code{type = "refresh_token"}. Defaults to \code{\link[=default_refresh_token]{default_refresh_token()}}.}

\item{token_file}{Path to the federated service-account token file. Used
only when \code{type = "workload_identity"}. Defaults to
\code{\link[=default_federated_token_file]{default_federated_token_file()}} (\code{AZURE_FEDERATED_TOKEN_FILE}).}

\item{oauth_host}{Azure authority host. Used only when
\code{type = "workload_identity"}. Defaults to \code{\link[=default_azure_host]{default_azure_host()}}.}
}
\value{
A named list of Hadoop \verb{fs.azure.*} key-value pairs.
}
\description{
Builds the named list of Hadoop \verb{fs.azure.*} configuration keys required to
authenticate Apache Spark (or any ABFS-compatible runtime) against Azure Data
Lake Storage Gen2 via OAuth 2.0. The returned list can be passed directly to
\code{SparkSession.conf.set()}, a Databricks cluster's Spark config, or any other
mechanism that accepts key-value Hadoop properties.

Three authentication types are supported:
\describe{
\item{\code{"client_secret"}}{Service principal with client secret
(\code{ClientCredsTokenProvider}).}
\item{\code{"refresh_token"}}{Delegated user identity via a refresh token
(\code{RefreshTokenBasedTokenProvider}).}
\item{\code{"workload_identity"}}{Kubernetes workload identity via a projected
service-account token file (\code{WorkloadIdentityTokenProvider}). Requires
Hadoop 3.4.1+ / 3.5.0+ (HADOOP-18610). Suitable for AKS pods with
workload identity enabled.}
}
}
\examples{
# Global client-secret config (applies to all storage accounts)
azure_spark_storage_conf(
  type = "client_secret",
  tenant_id = "my-tenant",
  client_id = "my-client",
  client_secret = "my-secret"
)

# Scoped to a specific storage account
azure_spark_storage_conf(
  type = "client_secret",
  storage = "mystorageaccount",
  tenant_id = "my-tenant",
  client_id = "my-client",
  client_secret = "my-secret"
)

\dontrun{
# Workload identity on AKS (reads env vars automatically)
azure_spark_storage_conf(type = "workload_identity")
}
}
