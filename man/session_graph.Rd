% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_session.R
\name{session_graph}
\alias{session_graph}
\title{Create Microsoft Graph API Session}
\usage{
session_graph(version = "v1.0", .chain = NULL)
}
\arguments{
\item{version}{A character string specifying the Microsoft Graph API
version to use. Defaults to \code{"v1.0"}. Common values are \code{"v1.0"} for
the stable API and \code{"beta"} for preview features.}

\item{.chain}{An optional credential chain object to use for authentication.
If \code{NULL}, uses the default credential chain. See \code{\link[=get_request_authorizer]{get_request_authorizer()}}
for details.}
}
\value{
An \link{api_session} object configured for Microsoft Graph API
}
\description{
Creates a new API session configured for Microsoft Graph API with
automatic Azure authentication. This is a convenience function that
sets up the correct host URL and credentials for accessing Microsoft
Graph services.
}
\details{
The function automatically configures:
\itemize{
\item Host URL: \verb{https://graph.microsoft.com/\{version\}}
\item Scope: Default Azure Graph scope
\item Tenant: Default Azure tenant ID
\item Credentials: Azure request authorizer with the configured scope and tenant
}
}
\examples{
\dontrun{
# Create a session for Microsoft Graph v1.0 API
graph <- session_graph()
graph$.client$.fetch("me")

# Create a session for Microsoft Graph beta API
graph_beta <- session_graph(version = "beta")

# Use the session to make API calls
# users <- graph$.client$.fetch("/users")
}
}
