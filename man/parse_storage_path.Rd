% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/storage-path.R
\name{parse_storage_path}
\alias{parse_storage_path}
\title{Parse an Azure Storage path}
\usage{
parse_storage_path(path)
}
\arguments{
\item{path}{A character string containing the Azure Storage path to parse.}
}
\value{
An \code{azure_storage_path} object (a named list) with the fields:
\describe{
\item{\code{scheme}}{URL scheme, e.g. \code{"abfss"}, \code{"wasbs"}, \code{"https"}.}
\item{\code{storage_account}}{Storage account name.}
\item{\code{endpoint}}{Storage endpoint type: \code{"dfs"} or \code{"blob"}.}
\item{\code{endpoint_suffix}}{Host suffix after the endpoint label, e.g.
\code{"core.windows.net"} (Azure public), \code{"core.usgovcloudapi.net"}
(US Government), \code{"core.chinacloudapi.cn"} (China), or
\code{"storage.azure.net"} (DNS-zone endpoints). Use this to distinguish
sovereign clouds from the public cloud.}
\item{\code{container}}{Container name. Called "filesystem" in ADLS Gen2 /
ABFS contexts; both refer to the same underlying object.}
\item{\code{path}}{Path within the container, without a leading \code{/}. Empty
string if the URL points to the container root.}
\item{\code{format}}{Inferred dataset or file format (see above).}
\item{\code{query}}{Named list of query parameters (e.g. a parsed SAS token),
or \code{NULL} if none.}
\item{\code{original}}{The original input string.}
}
}
\description{
Splits an Azure Storage URL or Hadoop filesystem path into its constituent
parts. Supports all common Azure Storage path formats:

\describe{
\item{\verb{abfss://} / \verb{abfs://}}{Azure Data Lake Storage Gen2 (DFS endpoint),
used by Spark / Hadoop.}
\item{\verb{wasbs://} / \verb{wasb://}}{Legacy Azure Blob filesystem scheme, used by
older Spark / Hadoop integrations.}
\item{\verb{https://} / \verb{http://}}{Standard Azure Blob or DFS REST endpoint,
optionally with a SAS token query string.}
\item{\verb{az://} / \verb{azure://}}{Non-standard aliases used by some Python tools
(e.g. \code{adlfs}); parsed using the same \code{container@account.host/path}
shape as \verb{abfs://}.}
}

The \code{format} field is inferred from the path on a best-effort basis:
\code{"delta"} when the path contains \verb{_delta_log}; a file extension name
(\code{"parquet"}, \code{"csv"}, \code{"json"}, \code{"avro"}, \code{"orc"}, \code{"text"}) when the
last path segment has a recognised extension; \code{"folder"} when there is no
extension; and \code{NA} for unrecognised extensions.
}
\examples{
parse_storage_path(
  "abfss://mycontainer@myaccount.dfs.core.windows.net/data/sales/2024"
)

parse_storage_path(
  "https://myaccount.blob.core.windows.net/mycontainer/data/events.parquet"
)

parse_storage_path(
  "wasbs://mycontainer@myaccount.blob.core.windows.net/data/delta_table"
)
}
