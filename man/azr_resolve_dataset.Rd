% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/az-dataset.R
\name{azr_resolve_dataset}
\alias{azr_resolve_dataset}
\title{Build a URI + format manifest for an \code{azr_dataset} or \code{azr_catalog}}
\usage{
azr_resolve_dataset(x, ...)
}
\arguments{
\item{x}{An \link{azr_dataset} or \link{azr_catalog} object.}

\item{...}{Additional arguments passed to methods:
\describe{
\item{\code{tier}}{Environment tier name (a key in the dataset's
\code{storage}). Defaults to the \code{dataset_tier} option
(\code{options(azr.dataset_tier = ...)} or \code{AZR_DATASET_TIER}, default
\code{"prod"}); see \code{\link[=azr_options]{azr_options()}}.}
\item{\code{uri_type}}{URI type: \code{"https"} or \code{"hadoop"} (the Hadoop ABFS
URI form \verb{scheme://container@account.dfs.../path}, used by Spark,
Flink, Trino, and any \code{hadoop-azure} consumer).}
\item{\code{name}}{For an \link{azr_catalog} only: an optional character scalar
selecting a single dataset by name. If omitted, URIs for every
dataset are returned.}
}}
}
\value{
For an \link{azr_dataset}, or an \link{azr_catalog} with \code{name} supplied, an
\link{azr_dataset_manifest}. For an \link{azr_catalog} without \code{name}, a named list
of \code{azr_dataset_manifest} objects, keyed by dataset name.
}
\description{
Like \code{\link[=azr_dataset_uri]{azr_dataset_uri()}}, but each entry also carries the dataset's \code{format},
which together are what a reader (e.g. \code{sparklyr::spark_read_source()})
needs to load a dataset.
}
\examples{
ds <- azr_dataset(
  name = "orders",
  scheme = "abfss",
  container = "raw",
  storage = list(prod = "stprod001"),
  path = "sales/orders",
  format = "delta"
)
azr_resolve_dataset(ds, tier = "prod")

catalog <- azr_catalog(datasets = list(ds))
azr_resolve_dataset(catalog, tier = "prod")
}
