% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{az_config}
\alias{az_config}
\title{Report the Azure environment configuration}
\usage{
az_config()
}
\value{
A \code{data.frame} with one row per environment variable and the
columns:
\describe{
\item{\code{variable}}{Name of the environment variable.}
\item{\code{value}}{The resolved value, or \code{NA} when the variable is unset
and has no built-in fallback.}
\item{\code{source}}{Where the value came from: \code{"env"} when read from the
environment, \code{"default"} when it falls back to a built-in value, or
\code{"unset"} when the variable is not set and has no fallback.}
}
}
\description{
Reports the Azure environment variables used during credential discovery,
the value each one resolves to, and where that value came from. This is the
same information shown in the package startup banner.

\code{AZURE_CLIENT_SECRET} is never reported verbatim: when it is set, its value
is returned as \verb{<<REDACTED>>}.
}
\examples{
az_config()

# which variables are actually set in the environment?
config <- az_config()
config[config$source == "env", ]
}
\seealso{
\code{\link[=default_azure_tenant_id]{default_azure_tenant_id()}}, \code{\link[=default_azure_client_id]{default_azure_client_id()}},
\code{\link[=default_azure_config_dir]{default_azure_config_dir()}}
}
